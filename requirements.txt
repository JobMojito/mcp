# JobMojito MCP server dependencies
# Prefect Horizon auto-installs these from requirements.txt at deploy time.
# 3.4.3 shipped a Host/Origin guard that 421s direct/reverse-proxy deploys; 3.4.4 reverts it.
# NOTE: lazy_auth.py hooks two FastMCP internals (AuthProvider.get_middleware()
# splicing and RequireAuthMiddleware's missing-header short-circuit). Neither is
# a public contract, so keep the upper bound and let
# tests/test_listing_readiness.py gate any upgrade.
fastmcp[apps]>=3.4.4,<4
httpx>=0.27
python-dotenv>=1.0
# MCP analytics + exception reporting. Optional at runtime: posthog_analytics.py
# degrades to a no-op if the import fails, so a deploy without it still boots.
posthog>=7.42
